# Project Instructions

## Simulation Runs

- Before running any simulation, run `conda env list`.
- Activate the conda environment whose name or path corresponds to `lifqif`, then run the simulation in that environment.
- Do not wait for full simulation completion unless explicitly requested.
- For long simulations, run only until there is enough output to confirm that it starts and progresses normally, then interrupt within 1 minute.
